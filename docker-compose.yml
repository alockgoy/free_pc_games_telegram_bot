version: '3.8'

services:
  free-games-bot:
    build: .
    container_name: free-games-bot
    restart: unless-stopped
    environment:
      - TZ=Europe/Madrid
    volumes:
      - ./data:/app/data
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge